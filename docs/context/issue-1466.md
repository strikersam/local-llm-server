# Issue #1466: crispy-burn-in: CRISPY promotion readiness

_Generated: 2026-09-07_

## Context Plan — Issue #1466: crispy-burn-in: CRISPY promotion readiness

> Auto-generated by the **issue-context-generator** workflow.
> This is a **DRAFT PR** — implement by triggering the Process Quick Note
> workflow or by opening a Claude Code session on this branch.

---

## Source Grounding

| | |
|---|---|
| Source URL | https://local-llm-server.onrender.com/api/autonomy/status` |
| Fetch status | ⚠️ **NOT FETCHED** — the plan below is unverified against the source |
| Generated by | NVIDIA NIM · `nvidia/nemotron-3-ultra-550b-a55b` |
| Repair rounds | 1 |
| Rulebook | `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md` |

---

## What the source actually is

The linked artifact is an auto-generated GitHub issue from the `.github/workflows/crispy-burn-in-check.yml` workflow. It reports the current burn-in status of a system called 'CRISPY' by querying `https://local-llm-server.onrender.com/api/autonomy/status` for `crispy_run_history`. The evaluation shows CRISPY has 0 total runs (threshold ≥20), 0% success rate (threshold ≥80%), no observation window (threshold ≥7 days), zero phase sequence errors (passing), and a pending risky-module-review sign-off. The issue explicitly states 'No action needed until the gap reports "All auto-checkable burn-in criteria met."' and that the next weekly check will update the issue.

---

## Decision

🛑 **REJECT** — nothing here belongs in this repository.

This is a status monitoring issue, not a feature request or bug. The CRISPY burn-in system appears to be already implemented and operational — the workflow automatically evaluates promotion readiness criteria (total_runs, success_rate, window_days, phase_sequence_errors, risky_module_review_signoff) and reports status. The issue body explicitly states no action is needed. There is no code change to adopt, adapt, or implement; the system will self-resolve when burn-in criteria are naturally met through continued operation.

---

## What was considered

No implementation needed. This issue is an auto-generated observability report from the CRISPY burn-in workflow. The CRISPY system (likely an autonomous agent evaluation framework) tracks its own promotion criteria via `/api/autonomy/status` and the weekly workflow updates this issue. When the burn-in criteria are naturally satisfied through continued system operation (accumulating ≥20 runs, ≥80% success rate over ≥7 days, and obtaining risky-module-review sign-off), the workflow will automatically update the issue to indicate promotion readiness. No code changes, tests, or configuration updates are required in this repository.

---

## Architectural Notes

Assuming CRISPY is an internal autonomous agent evaluation/burn-in system (unverified — the codebase context doesn't explicitly name it, but the workflow file `.github/workflows/crispy-burn-in-check.yml` and the `/api/autonomy/status` endpoint suggest it exists). If the CRISPY system itself needs changes to generate more runs or improve success rates, that would be a separate issue. The current issue is purely a status dashboard.

---

## Quality Gate

⚠️ **2 unmet rule(s)** from `docs/QUICK_NOTE_CONTEXT_RULEBOOK.md`. This plan was shipped anyway so a reviewer can see exactly where it is thin — treat the items below as unresolved before implementing.

- **R1** — the linked source was not retrieved, so every claim about it is unverified; the document must be reviewed before implementation
- **R10** — uses the former project name 'local-llm-server'; this repository is 'autonomous-ai-agency'

---

*Closes #1466*

