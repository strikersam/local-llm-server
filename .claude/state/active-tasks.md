# Active Task Tracker

> **Living document** — updated by every agent session across all tools (Claude Code, Codex, Cursor, Aider, etc.)
> Rules: mark IN_PROGRESS when you start a task, DONE when verified, BUG_FOUND when you discover an issue,
> BUG_FIXED when resolved. Never delete rows — when a row closes, move it to
> `.claude/state/archive/` rather than deleting it.
>
> Only open work lives here. 63 closed rows and the full session log were moved to
> [`archive/completed-2026-06-to-08.md`](archive/completed-2026-06-to-08.md) on 2026-08-10 —
> the SessionStart hook injects the top of this file into every session, so finished work
> was being paid for on every task.
>
> **The six `IN_PROGRESS` rows below were last touched between 2026-06-05 and 2026-07-03.**
> Their branches may already be merged. Verify status before picking one up; none of them
> was re-verified during the archive split.

## Status Key

| Status | Meaning |
|--------|---------|
| `TODO` | Planned but not started |
| `IN_PROGRESS` | Being worked on this session |
| `DONE` | Implemented, tested, merged |
| `BLOCKED` | Waiting on external dependency |
| `BUG_FOUND` | Bug discovered during implementation |
| `BUG_FIXED` | Bug confirmed fixed (link the PR) |
| `DEFERRED` | Deprioritised — see Notes for why |
| 39 | Make the agency pick work up automatically: Dependabot merges, exhaustion analysis, orphan recovery | `DONE` (merged; backlog draining) | [#1362](https://github.com/strikersam/autonomous-ai-agency/pull/1362) → master `25c5a07`; [#1363](https://github.com/strikersam/autonomous-ai-agency/pull/1363) → master `5fec638` | Three gates stacked behind one another, each looking like success from outside. (a) `secrets.GH_PAT` is always empty on a `dependabot[bot]` event — GitHub withholds repo secrets from that actor — so `GH_TOKEN` expanded to `""` and the merge step died with exit 4 on every PR it ever saw; `fetch-metadata` succeeded on the same event because it uses `GITHUB_TOKEN`. Fixed in #1362. (b) Arming auto-merge is necessary but not sufficient: all 14 PRs were `BEHIND` their base and branch protection refuses an out-of-date branch, so the sweep reported success and changed nothing. Fixed in #1363 with `gh pr update-branch`. (c) Those update-branch merge commits were attributed to `github-actions[bot]`, and GitHub does not auto-run workflows for those — all 10 resulting runs came back `action_required`, so the update *replaced* green required checks with pending ones (PR #1345: run 32696410024 passed, run 32934966004 `action_required`). Sweep now authenticates with `secrets.GH_PAT`, which it can do precisely because it runs on schedule/dispatch, not on the dependabot event — the two jobs use different tokens on purpose, pinned by `tests/test_dependabot_sweep_workflow.py`. Also: `mergeStateStatus: UNKNOWN` (lazy mergeability computation; 4 of 14 hit it) is re-polled rather than silently skipped, and the sweep no longer arms auto-merge on a PR it cannot classify — only the event-driven job has the payload `fetch-metadata` needs, so majors (`anthropic >=1.0.0` #1336, `reportlab` #1346) stay with a human per rule 40. Orphan sweep ran for real: open issues 3 → 10. **Closed 2026-08-26 10:40.** Two more gates surfaced only by verifying outcomes rather than exit codes. (d) `gh pr update-branch` under `GITHUB_TOKEN` produces commits attributed to `github-actions[bot]`, and GitHub does not auto-run workflows for those: all 10 resulting runs came back `action_required`, so the update traded green required checks for pending ones (PR #1345, run 32696410024 passed vs 32934966004 `action_required`). Fixed in #1364 by authenticating the sweep with `secrets.GH_PAT` — available to it because it runs on schedule/dispatch, not the `dependabot[bot]` event. Proven: same branch, run 32936354170, actor `strikersam`, ran normally. (e) That token change silently disabled #1364's own rule-40 guard: the event-driven job is gated on `actor == dependabot[bot]`, so it now skips (#1336, run 32936415712, `skipped`) and never writes the major-bump marker the sweep checked for. #1365 gives the sweep a verdict it owns (`scripts/classify_dependabot_update.py`, 30 tests) and makes the sweep hourly, updating at most one stale branch per run — branch protection means only one PR can merge per run anyway, so refreshing the rest fired 24 CI runs and discarded 23. Verified live on run 32959137033: 12 PRs seen, 1 updated, 11 deferred, 0 warnings. Merged: #1362 `25c5a07`, #1363 `5fec638`, #1364 `bb3e06d`, #1365 `dd5c20f`. Dependabot backlog 14 → 12 (#1346, #1345 merged), draining ~1/hour unattended. #1336 (`anthropic >=0.122.0 → >=1.0.0`) deliberately held: auto-merge disabled by hand, comment posted, needs a human per rule 40. Agency loop converged: run 380 ran on `50b04aa7` (contains the parser fix `cfea9ff`) and filed no new failure issue; #1331 and #1317 closed themselves, #1312 is on `retry:1`. Open issues 10 → 8. | 2026-08-26 |
| 40 | Why the issue backlog was never implemented: the implementer could not succeed, and its failure looked like success | `DONE` (fix pushed; PR [#1368](https://github.com/strikersam/autonomous-ai-agency/pull/1368)) | `claude/render-bottleneck-alternatives-sv8aj1` | **Correction to an earlier session's claim:** GitHub Actions was never down or disabled. That conclusion came from sampling `in_progress: 0` at four moments and over-generalising from 7 stuck queued runs; `process-quick-note.yml` succeeded at 2026-08-26T16:52, 23:16 and 2026-08-27T08:53, and four Dependabot PRs merged overnight unattended. The real cause is narrower and worse. `.github/scripts/implement_agent.py:457` refuses `IMPLEMENTATION_COMPLETE` unless the last `pytest` exited 0 (`last_pytest_passed`), and `process-quick-note.yml` ran with **no MongoDB service** while `ci.yml` has declared one all along — so `tests/test_auth_me_regression.py` errors on `localhost:27017`, `pytest -x` stops there, and that gate was unsatisfiable on *every* issue no matter what the agent wrote. This is defect (c) from row 38, fixed there for `agency-cycle.yml` and `continuous-improvement.yml` and never applied to the workflow that does the implementing. It was invisible because a barren run *skips* rather than fails: run 33056034069 on #1319 showed the loop "succeed", steps 9–14 skipped, the retry handler bump retry:1 → retry:2, and a green tick. `ci-failure-autofix.yml` had the same gap — it verifies its candidate patch with a full suite run and abandons on failure, so it could never ship a fix either. Both now declare `mongo:7`; a final step exits 1 when the retry handler fires, so a barren run is red; and `tests/test_process_quick_note_workflow.py` (16 tests, 11 verified failing first) asserts the invariant repo-wide — every job running the full suite must declare Mongo, so the next such workflow cannot be written without it. Triage: all 5 open escalation issues (#1312, #1319, #1328, #1349, #1360) run through `scripts/analyze_exhaustion.py` → `blocked-infrastructure` on every one; closed `not_planned` with the verdict recorded. Open issues 8 → 3. | 2026-08-27 |
| 41 | Second root cause: the implementer's hardcoded model list was entirely dead | `DONE` (fix on branch) | `claude/render-bottleneck-alternatives-sv8aj1` | Fixing the missing MongoDB (row 40) was necessary but not sufficient. The first run after that merge went **red via the new fail-loudly step** — which is the step working as intended: it converted a silent green into a precise, actionable failure on the very first try. The log named the real cause: `.github/scripts/implement_agent.py` held six hardcoded NVIDIA model ids and its own failover (violating rule 2), and all of them were dead — `nvidia/llama-3.3-nemotron-super-49b-v1.5` and `meta/llama-3.3-70b-instruct` `410 Gone` as of 2026-08-26T09:00, `qwen/qwen3-coder-480b-a35b-instruct` since 2026-06-11, `qwen/qwen2.5-coder-32b-instruct` since 2026-05-12, and `nvidia/llama-3.1-nemotron-ultra-253b-v1` a `404`. Turn 1: "All candidate models exhausted", 59s, no diff. Now routed through `ProviderRouter`, which already had the Cerebras → Groq → NVIDIA → Ollama chain and rule 4's 410 dead-model tracking that this script reimplemented none of. **Non-obvious constraint found while wiring it:** `packages/ai/router.py` has zero `tools`/`tool_calls` handling — the `anthropic`, `bedrock` and ollama-native paths convert the payload and would drop tool definitions *silently*, so the router is filtered to `openai-compatible` providers only. Also: the response cache keys on (model, messages, temperature, max_tokens, stop) and **not** on tools, so `temperature` is left unset to keep it disengaged. `allow_commercial_fallback=False` preserves the recorded decision that this loop never escalates to a paid provider silently. 17 tests, verified failing first; one caught a real slip I introduced (SDK attribute access `tc.id` left on a now-dict `tool_calls`, which would have raised on the first tool call of every run). | 2026-08-27 |
| 42 | The loop's own merges landed on master with no issue number, and the endpoints it shipped were undocumented | `DONE` (fix on branch) | `claude/render-bottleneck-alternatives-sv8aj1` | First end-to-end autonomous run (run 33204279915, 39 min, issue #1356) succeeded at every step and squash-merged `b368f9e7`. Two defects in what it produced. (a) **Empty issue number.** The merge step builds its squash subject from `$ISSUE_NUM` and was the one step in `process-quick-note.yml` whose `env:` never declared it; under `bash -e` that expands to empty, so `--subject` was passed, the merge succeeded, the run was green, and the only evidence was in `git log`. Declared it from the same `steps.issue.outputs.number` every other step reads, and it now omits `--subject` rather than writing a dangling `#`. `tests/test_workflow_shell_vars_are_declared.py` generalises the guard: every variable a `run:` block reads must have a source (step/job `env:`, an earlier `$GITHUB_ENV` write, a local assignment, or an explicit `${VAR:-default}`). Across the 41-workflow fleet that scan finds this and nothing else; it fails first on the workflow as it stood when it merged the empty subject. (b) **Rule 37 unmet.** `docs/api-surfaces.md` had no SEO section at all — all ten routes in `backend/seo_api.py` were undocumented, not only the three new ones. Added, with the auth posture the code implements. **Audit of `b368f9e7` itself:** rule 10 ✅ (all three endpoints `Depends(_get_current_user_thunk)` *and* `get_company_access`), rule 11 ✅ (Pydantic v2 request models with `Field` constraints, `response_model` on each). Rule 28 ✗ — `build_seo_roadmap` 74 lines, `plan_seo_sprint` 83, `run_seo_pipeline` 114, plus three bridge functions at 52/57/78, all over the 50-line limit; `backend/seo_api.py` grew 381→756 lines against the 800-line cap. Not refactored here: that is behaviour-touching work on code merged hours ago, and it needs its own change. **Corrected after verifying against GitHub:** PR #1357 is not a separate earlier PR — it *is* the PR that produced `b368f9e7` (merged_at 20:10:59Z, base `8b448842`, 1199/7/7 matching the commit). Its title still reads "docs: reject: … out of scope" and its body still says "🛑 REJECT — nothing here belongs in this repository"; the implement step ran on the same branch and merged 1,199 lines under it. The reject was itself ungrounded — the same body records "Fetch status: ⚠️ NOT FETCHED" and an unmet rule R1 — so planner and implementer both worked from an article neither had read. The empty squash subject is what hid the contradiction. Review bots did not review: CoderRabbit declines this repo (<10 stars), Codex was out of quota; the council step returned **WARN** including "cannot verify authentication/authorization guards" and "require human verification before merge", and the workflow treats WARN as mergeable. Both WARNs resolved by hand: security is clean (rule 10 holds), but **correctness found a real defect** — `estimated_monthly_value` is not a field on `Initiative` and is dropped in `delegation_task_to_initiative`, so all three new endpoints return `0` for it and the roadmap prints `$0`, hidden by `getattr(init, 'estimated_monthly_value', 0)` at 10 call sites. Confirmed by construction. Fixing it is rule 1 work → left for a decision. | 2026-08-29 |
| 43 | Drive issues and PRs to zero, and close the five gates that let bad work through | `DONE` (fix on branch) | `claude/render-bottleneck-alternatives-sv8aj1` | **Backlog cleared: 6 issues → 0, 11 PRs → 0.** Four `agency-escalation` issues (#1312, #1319, #1328, #1349) escalated the same two non-failures — pytest captured log records read as node IDs, and a MongoDB refusal from a service the loop workflows did not declare — both fixed in `cfea9ff`; verified by reading all four bodies, not by pattern-matching titles. #1350 was a trend digest (reviewed: nothing actionable; the five Ollama items are not dependency upgrades here — no pinned client or server version, it is reached over HTTP at `${OLLAMA_BASE}`). #1347 reported "not ready for promotion" with an **empty gap and empty evaluation JSON** — a verdict computed from no data, because Render is suspended and two `grep … | cut … || echo` fallbacks could never fire. Eight draft context PRs closed (two orphaned — their issues closed days earlier). #1334 merged green; #1335/#1336 conflicted, so both pins were applied directly. **`anthropic>=1.0.0` is not a breaking upgrade in practice:** `>=0.122.0` already resolved to 1.0.0, so CI has run it since release; both `agent/loop.py` call sites use only parameters 1.0.0 still accepts, and neither passes the removed `temperature`. **Then the root causes:** the implement step never read the plan's own REJECT; `WARN` auto-merged; a *crashed* council defaulted to `WARN` and merged; the review-bot wait reported success against zero reviews; the queue took the oldest open issue of any kind. New `scripts/context_plan_gate.py` fails closed on reject/ungrounded/unmet-rules/unknown. 48 tests, 25 of which fail against the pre-fix workflows. | 2026-08-29 |
| 44 | The catalogue probe could not answer the question it exists to answer, and Cerebras was configured entirely from ids that do not exist | `DONE` (fix on branch) | `claude/render-bottleneck-alternatives-sv8aj1` | `CEREBRAS_API_KEY` reached Actions, so PR #1378's claim could finally be checked. It could not be: `probe_catalogues.py` **skipped the chat call entirely whenever `list-models` failed** — the tool whose stated premise is *listing is not proof, answering is* let a listing failure veto the answering, discarding the explicit `--model` ids passed to test the candidate. Fixed: listing and calling are independent, a provider counts as reachable if either succeeds, and *answered but would not list* is its own summary line rather than being collapsed into `unreachable` — collapsing those is how a live model id got written off the day before. Relaxing that exposed a second silent path: `probe_chat` returned `True` for an adapter kind it has no chat route for, which would now read as an answer; the skip is decided before the call and counts as nothing. Then the run still 403'd on everything — **because `urllib` sends `User-Agent: Python-urllib/3.13`**, which Cerebras' edge rejects, producing a 403 indistinguishable by status from a revoked key. With a real User-Agent the account answered, and the answer was worse than the PR: its `/v1/models` holds exactly `gpt-oss-120b` and `gemma-4-31b`, and **all four ids in play answered 404** — `qwen-3-coder-480b` (the default, named in `CLAUDE.md`), `llama-3.3-70b` (`ProviderRouter`'s default and the verifier/judge preset), `llama-3.1-8b`, and #1378's `qwen-3-235b-a22b-instruct-2507`, which would have gone in at `priority: 9` ahead of everything. Rule 4 makes Cerebras the first provider tried on every call, so this was a 404 on the first hop of every request, reported by nothing, because failing over to Groq is indistinguishable from a healthy chain. Configuration now names only what the account lists, across eight files; the two Cerebras copies are reconciled, which the `KNOWN_DIVERGENT` staleness guard caught and which now extends the reconciled-provider test to cover Cerebras as well as NVIDIA. Nothing is claimed to work: both remaining ids return **402 Payment Required** — an account state, not a model state — so capability numbers are conservative floors and `supports_tools` is `false`. #1378 closed with the probe output on the thread. 51 new tests (7 probe, 44 catalogue); 5 and 21 respectively fail against the pre-fix code. | 2026-08-29 |

---

## Current Sprint Tasks

| # | Task | Status | PR / Branch | Notes | Updated |
|---|------|--------|-------------|-------|---------|
| 53 | Daily automation 2026-09-06: catalogue probe stopped flagging disabled local providers as unreachable | `IN_PROGRESS` (PR open, auto-merge armed) | [#1443](https://github.com/strikersam/autonomous-ai-agency/pull/1443) → `claude/upbeat-goodall-gm78vl` | Issue #1434 (open, `retry:1`) listed `ollama`/`lmstudio`/`vllm`/`localai` as "Unreachable providers." Root cause: those four default to a `localhost` `base_url` + `requires_key: false` even when `*_ENABLED` is off (`config/llm/providers.yaml`), so `.github/scripts/probe_catalogues.py`'s full sweep dialled them anyway on every scheduled CI run and always failed — permanent, unfixable noise, since they were never meant to be reachable in Actions. Reproduced directly (`config.providers["ollama"].enabled == False` but `base_url` truthy) before touching code. Fix: the full sweep now skips a provider whose `enabled` flag is off (`skipped — disabled here`); an explicit `--provider <id>` still probes it, so the documented candidate-check workflow (`catalogue-probe.yml -f provider=`) is unaffected. 3 new regression tests, verified failing first against the pre-fix script. **Not touched:** issue #1434's other two lines — `cerebras:gpt-oss-120b` HTTP 402 is the already-tracked billing hold (NEXT_ACTION.md §6), and `anthropic:claude-sonnet-5` HTTP 400 could not be reproduced or root-caused from this sandbox (no `ANTHROPIC_API_KEY` here); needs a live-key run to diagnose, left for next session. **Sandbox constraints hit:** no `fastapi`/`motor`/`bcrypt`/`pydantic` here, so `tests/conftest.py` and `agent/loop_registry.py` can't load — verified the changed test file directly with `pytest --noconftest` (34/34 pass) instead of the full suite; `pip install httpx pyyaml pytest` into the sandbox to get that far. `python -m compileall -q .` and `check_changelog_parity.py` both clean. | 2026-09-06 |
| 52 | Routine backlog W36 (#1422, all four items) + recover the mis-pointed web-reach/graphify PR | `DONE` (merged) | [#1424](https://github.com/strikersam/autonomous-ai-agency/pull/1424), [#1425](https://github.com/strikersam/autonomous-ai-agency/pull/1425), [#1426](https://github.com/strikersam/autonomous-ai-agency/pull/1426) | Executed issue #1422's four shortlisted items. **#1426 (item 1):** the `catalogue-probe.yml` diagnostic now runs on a **daily schedule** (kept `workflow_dispatch`) and files exactly one drift-tracking issue when a routed provider is unreachable or a named model won't answer — new `.github/scripts/probe_report.py` (create-vs-update by body marker, GitHub ops injected for unit tests), additive `--json` summary in `probe_catalogues.py`, `loops/registry.yaml` entry (rule 35), `issues: write` added (rule-40 permissions change, confirmed with the maintainer). **#1425 (items 2-4):** `_chat_json`'s retry-cap `ValueError` now carries the attempt count + a 200-char snippet of the last raw output (regression test verified failing first); a documenting test proves one positional Anthropic cache breakpoint covers the whole prefix incl. tool turns (verify-only, no code change); CLAUDE.md's stale "Cerebras = Free fast LLM" line corrected to paid-tier. **#1424:** recovered the web-reach `trust:untrusted-external` tagging + graphify hook self-heal that #1423 had drifted off of (#1423 closed, pointed at the already-merged #1411 prompts commit). Serialized the three graph-regenerating PRs so no two collided on `GRAPH_REPORT.md`. All shipped with tests, changelog parity, rule-39 graph rebuilds. | 2026-09-05 |
| 50 | Central provider/model source of truth + admin-UI control (health, base_url, key) — end the dead-ID churn | `IN_PROGRESS` (phase 0 shipped) | `claude/provider-health-status-3gc25r` | **Trigger:** Render logs showed `nvidia-nim/z-ai/glm-5.2 410 Gone` + a watchdog failover on every ~30-min cycle. Root cause is a split-brain: the admin Providers UI edits `get_db().providers` (Mongo) but the brain (`services/brain_failover.py`) ignores it — it builds from hardcoded `_PROVIDER_REGISTRY`+`PROVIDER_CANDIDATES` and reads keys/base_url from **env**; model ids live in 4 drifting catalogues (`config/llm/models.yaml`, `config/models.yaml`, `brain_config.PROVIDER_CANDIDATES`, `packages/ai/registry.py`) hardcoded across ~20 files. **Decisions (user):** (1) ONE authoritative model catalogue, everything derives from it; (2) keys **stay in Render env** (rule 6 preserved) but are **updatable from the UI via the Render API** — "Render acts as the key DB"; (3) UI shows availability + health + editable base_url/model, kept simple (ref: CompanyHelm/companyhelm, not yet reviewed). **Phase 0 DONE:** dead planner `prefer_models` fixed (506918b); rule 4 corrected; `scripts/check_model_catalog_consistency.py` + CI gate that hard-fails undeclared prefer_models / preset-not-in-candidates and warns on cross-catalogue divergence (11cd282) — proven to catch the `z-ai/glm-5.2` class. **Pending:** P1 unify catalogue (needs model→provider schema to allow one id on multiple providers — `packages/llm/registry.py:54` is one-provider-per-id today; 8 divergence warnings to resolve incl. `aerolink` Claude gateway + gpt-oss groq/nvidia); P2 brain reads unified catalogue for models; P3 UI→Render-env key/base_url update endpoint (foundation exists: `backend/render_router.py`, `packages/integrations/render_mcp.py`) + health surfaced from `/api/brain/failover/status`; P4 purge dead ids across the ~20 files (keep test poison fixtures). **Untestable in web container** (no backend deps: fastapi/bcrypt/motor) — P1-P4 must land with the real pytest suite. **Also flagged, operator action:** `anthropic-claude` 400s every cycle (paid, out-of-credit/bad-key) — not code. | 2026-09-03 |
| 2 | Dynamic Session Planning Workflow (this task) | `IN_PROGRESS` | [#406](https://github.com/strikersam/local-llm-server/pull/406) | hooks + tracker + AGENTS.md update | 2026-06-05 |
| 6 | Portfolio refresh workflow → reuse RENDER_BACKEND_URL secret | `IN_PROGRESS` | claude/portfolio-refresh-backend-url | point cron ping at existing secret | 2026-06-06 |
| 8 | FreeBuff always-on Telegram bot (24×7 Render/Docker, embedded mode) | `IN_PROGRESS` | `claude/freebuff-telegram-deploy` | embedded in-process agent + launcher + Dockerfile.telegram + render worker + deploy docs | 2026-06-06 |
| 11 | SEO audit: browser-use fetch (Akamai bypass), honest revenue model, demoable UI tab + downloads | `IN_PROGRESS` | `claude/cool-davinci-494siy` | services/seo_fetch.py (httpx/Playwright/auto-escalate), diminishing-returns revenue curve, CompanyScreen SEO tab + CSV/JSON/MD downloads; 13 new fetch tests. Live Akamai bypass needs Playwright browsers in deploy | 2026-06-13 |
| 27 | Adversarial autonomy-paths audit + hardening | `IN_PROGRESS` | #694 | 2 `agent/`-local fixes (self-heal re-dispatch stranding; broken Hermes dispatch) + 3 tests. Deferred orchestrator-zone findings → Bug Log #9-#12. | 2026-06-19 |
| 32 | Move heavy voice deps out of the web image → standalone Dockerfile.voice (slim web image, fast cold starts) | `IN_PROGRESS` | `claude/sam-voice-livekit-xjow4g` (PR #935 pending) | Dockerfile.backend no longer installs requirements-livekit (~600MB); new Dockerfile.voice (slim+libgomp1, backend deps, CMD worker start); docs/render.yaml/tests updated | 2026-07-03 |
| 47 | Approve/release 422 on the task board + Telegram approval taps looked dead | `BUG_FIXED` (PR #1406 → master `d1b8f1c`) | `claude/approve-release-telegram-bugs-eqhnh0` | (1) Board "→ Approve & release" POSTed `{approved:true}` to `/api/tasks/{id}/approve`, which needs `{checkpoint_id, approve}` — hence `checkpoint_id: Field required; approve: Field required`. `handleApprove` now approves each pending checkpoint with the canonical body, and releases a checkpoint-less reviewed task via PATCH→`done`. (2) Telegram Approve/Reject did nothing when only `TELEGRAM_ALLOWED_USER_IDS` (not `ADMIN`) was set: the card is delivered to ALLOWED but the callback requires ADMIN, and the denial was a fading toast on an unedited message (the one callback path that leaves the message untouched). Auth unchanged; denial is now a `show_alert` modal naming the env var + a `log.warning`. Not changed: the ADMIN-vs-ALLOWED asymmetry itself (widening who can approve is an auth change → left for a human). 2 frontend + 1 backend regression tests; frontend suite 154 green, telegram suites 51 green. | 2026-09-02 |
| 51 | self_heal was deleting the Telegram webhook every ~15 min → buttons died a quarter-hour after each deploy | `BUG_FIXED` | `claude/approve-release-telegram-bugs-eqhnh0` | User: "click approve & merge, nothing happens." Render logs were decisive: startup `Telegram webhook registered`, then `self_heal: cleared Telegram webhook` ~15 min later, then `telegram: {action: no_webhook}` every cycle — Telegram had nowhere to deliver taps. `services/self_heal.py::_heal_telegram` deletes any set webhook to unblock the getUpdates poller (409), but ran unconditionally and fought webhook mode: register at boot → self-heal deletes → dead until next restart (explains why each deploy "worked for a while"). Fix: bail out with `webhook_mode_skip` before any Telegram call when `TELEGRAM_WEBHOOK_ENABLED` is on. Regression test asserts zero Telegram HTTP calls in webhook mode. Verified state via Render MCP (service awake, background loops fine; no webhook POSTs because Telegram had no webhook to POST to). Files: `services/self_heal.py`, `tests/test_self_heal_v2.py`. | 2026-09-05 |
| 50 | Transient Telegram setWebhook DNS failure left the bot with no transport | `BUG_FIXED` | `claude/approve-release-telegram-bugs-eqhnh0` | User hit `setWebhook failed: Bad Request: bad webhook: Failed to resolve host: Temporary failure in name resolution`. Confirmed via Render logs (deploy 747ba4d, 14:23): "WEBHOOK mode — poller not started" + setWebhook failed → **no transport** (webhook not set, poller not started); buttons only survived if a prior deploy's webhook was still registered. Telegram resolves/probes the URL at registration and its resolver hiccups on cold start. Fixes: (1) `register_webhook` retries setWebhook 5× with exp backoff (idempotent; runs in a startup bg task so it never blocks the port); (2) on final failure, startup falls back to the getUpdates poller (run_bot deletes any stale webhook first → no 409), so there's always a transport. 2 new tests. Files: `telegram_bot.py`, `backend/server.py`, `tests/test_telegram_webhook.py`. | 2026-09-03 |
| 49 | Telegram webhook secret + bot token leaked into logs (regression from #1407's setWebhook) | `BUG_FIXED` | `claude/approve-release-telegram-bugs-eqhnh0` | Verified via Render MCP that webhook mode works (setWebhook→200, "webhook registered", poller not started, buttons live). But the startup log showed httpx's INFO line `GET .../bot<TOKEN>/setWebhook?secret_token=<SECRET>...` — both bot token (URL path) and the new secret_token (query) in cleartext logs (rule 6). Fixes: (1) `register_webhook` POSTs params in the JSON body (secret never in a URL); (2) `logging.getLogger("httpx").setLevel(WARNING)` at import kills the per-request INFO line that leaked the token on every call (pre-existing). Register test asserts secret in body, absent from URL. Rotation left to operator (their call). Files: `telegram_bot.py`, `tests/test_telegram_webhook.py`. | 2026-09-02 |
| 48 | Follow-up: Telegram "Approve & merge" (and every inline button) still did nothing — make buttons work via webhook + make the runtime cause visible | `BUG_FIXED` (PR #1407) | `claude/approve-release-telegram-bugs-eqhnh0` | User: "do whatever is needed to make the buttons work." Root cause is NOT the handlers (correct, route fine): the `getUpdates` long-poll consumer isn't draining updates. Sending a card is an independent direct `sendMessage` (`NotificationDispatcher`/`pr_approval_gate`), so a card arrives while every button is dead — unifying explanation across task-approve/wfo/pr-merge. Can't reach prod to flip config (egress denies `local-llm-server.onrender.com` 403; Render MCP down), so shipped the real fix in code: **webhook mode**. `TELEGRAM_WEBHOOK_ENABLED=true`+`TELEGRAM_WEBHOOK_SECRET` → startup `setWebhook` to `${RENDER_EXTERNAL_URL}/api/telegram/webhook` and the poller is NOT started (webhook 409s getUpdates). New `POST /api/telegram/webhook` validates `X-Telegram-Bot-Api-Secret-Token` (mismatch/unset→403; per-user admin gate still applies), dispatches to the same `_process_callback`/`_process_update` on a bg task, prompt 200. Default off; reverting to polling self-heals. Plus diagnostics: `_LAST_SUCCESSFUL_POLL_TS`/`poller_heartbeat_age_sec()`, `get_webhook_info()`, and `/api/telegram/diag` live fields (`poller_running_here`, `webhook{...}`, `buttons_do_nothing` hint). 15 new tests. Files: `telegram_bot.py`, `backend/server.py`, `tests/test_telegram_webhook.py`, `tests/test_telegram_diag_endpoint.py`, `.env.example`, `docs/configuration-reference.md`, `docs/api-surfaces.md`. | 2026-09-02 |
| 51 | Daily automation 2026-09-04: claude-fable-5-1 catalog + cost_tracker fix + reasoning_tokens | `DONE` | `claude/intelligent-gates-ctq0hw` | claude-fable-5-1 added to config/llm/models.yaml + cost_tracker; fable-5 cost_tracker pricing fixed (3.0/15.0→30.0/120.0); Usage.reasoning_tokens field + AnthropicProvider extraction; 27 new tests green | 2026-09-04 |
| 46 | Daily automation 2026-08-31: extended 1-hour prompt cache TTL + MCP 2026-07-28 routing headers | `DONE` | `claude/nifty-pasteur-akr9rb` | `ANTHROPIC_CACHE_TTL=1h` enables extended TTL on all 3 cache slots + extended-cache-ttl-2025-04-11 beta; MCPClient adds Mcp-Method/Mcp-Name headers; 27 new tests green | 2026-08-31 |
| 45 | Daily automation 2026-08-30: rolling conversation cache breakpoints in Anthropic provider | `DONE` | `claude/nifty-pasteur-dxyu3s` | `_add_conversation_cache_breakpoints` marks messages[-2] with cache_control so multi-turn agent loops pay prefix cost once; 17 tests green | 2026-08-30 |
| 33 | Daily automation 2026-08-21: Claude 5 YAML catalog + workspace-id observability | `DONE` | `claude/nifty-pasteur-4zlcyu` | claude-sonnet-5/opus-5/haiku-4-5-20251001 added to models.yaml; default promoted to sonnet-5; anthropic-workspace-id header captured; 20 tests; changelogs updated | 2026-08-21 |
| 34 | Add TokenIn (tokenin.my.id) as a free rotatable brain provider | `DONE` (code) / `BLOCKED` (Render+live check) | `claude/tokenin-provider-integration-57v8k6` | Provider wired across models.yaml, brain_config Literal+fallbacks, brain_failover registry+opus alias, router from_env+free classification; 10 free `myt/` models as failover rotation; 6 new tests + count-assertion fixes across 4 suites. **BLOCKED:** Render env-var add refused — service `local-llm-server` suspended for billing (`cannot deploy suspended service`); live opus check not possible from this sandbox (egress policy blocks `tokenin.my.id`, 403 CONNECT). | 2026-08-22 |
| 35 | Frontend UX redesign: 20→6 IA, retire legacy UI, unify tokens | `DONE` (merged) | `claude/agency-ui-ux-redesign-yjvb85` (PR #1325 → master 67bd7f8) | Design canvas (Artifact) + implemented: 6 goal-based hubs w/ back-compat alias map for old /v5/* deep links; legacy /legacy dashboard + 21 pages + 6 dead components removed (~23k LOC); tokens unified to index.css (Tailwind Outfit→Manrope fix); new StatusPill/Spinner/HubTabs primitives. Fixed test_frontend_deployment_guards OAuth-origin guard (repointed from deleted SettingsPage.js → api.js — the one CI failure, unrelated to render). Squash-merged to master 2026-08-22 after governance #1324 landed first. | 2026-08-22 |
| 36 | Governance enforcement GA: policy+budget on every execution path, matrix STABLE | `DONE` (merged) | `feat/governance-enforcement-ga` (PR #1324 → master 9605f67) | GovernanceGate.charge feeds tokens/cost/depth; runtime dispatch runs the session budget; Surface.AGENT for spawn depth; all six ceilings fire independently; approvals fail-closed (TTL+restart); policies_governance→STABLE + policy_authoring_ui BETA; matrix-guard test. Squash-merged to master 2026-08-22. | 2026-08-22 |
| 37 | Policy Authoring UI GA: propose-via-PR editor, matrix STABLE | `DONE` (merged) | `feat/policy-authoring-ui-ga` (PR #1329 → master ff6ecd5) | New `packages/governance/authoring.py` validates a proposed policy against the real PolicyEngine + org baseline, diffs it, and opens a PR (never writes the live file). Router adds admin-only `GET /policy/raw`, `POST /policy/validate`, `POST /policy/propose` (needs GH_PAT); Governance screen gains a load→validate→propose editor. Org baseline can be tightened from the dashboard but never loosened (refused before any PR). `policy_authoring_ui`→STABLE + matrix-guard test; 30 authoring tests; docs + both changelogs. One CI fix (mounted the editor component — CRA no-unused-vars). Squash-merged to master 2026-08-23. | 2026-08-23 |
| 38 | Unblock the autonomous loop: real failing-test node IDs + a database for the loop workflows | `DONE` (merged) | [#1358](https://github.com/strikersam/autonomous-ai-agency/pull/1358) → master `cfea9ff` | Four root causes, each masked by the previous one. (a) `agency-cycle.yml`'s `grep -E '^(FAILED\|ERROR) '` matched pytest captured log records (`LEVEL logger:file.py:lineno`), emitting bogus IDs like `qwen-proxy:app_settings.py:70` that no pytest run resolves — the agent never converged and escalated every run (#1354). (b) `continuous-improvement.yml`'s `grep '^FAILED '` dropped ERROR summary lines, so error-only runs reported nothing (#1352). Both now call new unit-tested `scripts/parse_pytest_failures.py`. (c) Those two workflows declared no `mongo:7` service while `ci.yml` always had one, so only they saw `ServerSelectionTimeoutError` — the PR gate stayed green while the loop failed daily. Both now declare it. (d) Six modules set `ADMIN_EMAIL` at import after `backend/server.py` had captured a different value → seed 200 then login 401; conftest pins it. Plus `MEMORY_KERNEL_DIR` per-session temp dir. **Recorded trap:** pinning `STORAGE_BACKEND=sqlite` clears the error but hangs the suite — a non-daemon aiosqlite `_connection_worker_thread` stops interpreter exit (51+ min vs master's 3m27s, on a job with no `timeout-minutes`); tried, caught pre-merge, reverted. 30 new tests. | 2026-08-26 |

---

## Bug Log

| # | Bug Description | Found | Fixed | PR | Status |
|---|----------------|-------|-------|----|--------|
| 9 | Agent `write_file` workspace-isolation leak: `tests/test_e2e_agent_chat.py::TestAgentFullPRWorkflow::test_agent_full_pr_workflow` monkeypatches `_CHAT_AGENT_WORKSPACE_ROOT` to `tmp_path` and mocks an executor `write_file("src/main.py", "def hello(): return 'Hello Agent'")` step. Running the full suite (`pytest -q --ignore=tests/e2e`, 2026-06-14) left a real `src/main.py` with that exact content at the repo root — the agent write escaped the isolated `tmp_path` workspace into the live checkout. Root cause not yet isolated (suspects: `agent/loop.py` MCP-first `write_file` dispatch ~line 1051, or `WorkspaceTools` default-root fallback in `agent/tools.py`). Needs `risky-module-review`. | 2026-06-14 | — | — | `BUG_FOUND` |
| 16 | **GATE BYPASS (P0, DEFERRED):** `requires_approval` only redirects the final DONE→IN_REVIEW transition (`tasks/service.py:755`; `tasks/store.py:194` `list_pending` has no `requires_approval` filter), so the dispatcher runs the agent to completion BEFORE any approval. Mitigated by the agent's own autonomy gate (no merge/push to protected branches), but the charter's "gate risky/outward-facing BEFORE executing" needs a pre-EXECUTE gate. Architecturally significant + in the active orchestrator/telegram zone → needs an explicit decision. | 2026-06-19 | — | — | `BUG_FOUND` |
| 17 | **DEFERRED (orchestrator zone):** (a) `workflow_orchestrator.py` first-merge gate is coupled to `run.approved` not "this merge consented", so a restored/auto-approved run can skip it; (b) `services/repo_connection.py::decide_merge` returns `awaiting_repo_connection` with `requires_approval=False`, letting an auto-approved no-repo run execute-and-discard; (c) 16-hex signatures over `[:120]`-truncated inputs can collide and drop distinct heals/trends; (d) untracked `asyncio.create_task(ensure_self_company())` in `services/background.py` swallows exceptions. | 2026-06-19 | — | — | `BUG_FOUND` |
| 18 | **DEFERRED (P2, pre-existing, low severity):** Stale-company-ID recovery in both `CompanyScreen.jsx` (PR #962) and `CompanyGraphPanel` in `KnowledgeScreen.jsx` (PR #1110) validates the persisted `COMPANY_ID_KEY` against only the first page of `api.listCompanies()` (`limit=100` default, `backend/company_api.py:390`). An admin/owner with >100 accessible companies whose stored ID falls outside that first page gets wrongly treated as stale — cleared and silently replaced with `list[0]`. Flagged by Codex review on #1110 (https://github.com/strikersam/autonomous-ai-agency/pull/1110#discussion_r3633223301). Needs either full pagination until the stored ID is found, or a direct per-ID company/graph lookup before treating it as stale — should fix both screens together to avoid the two staying inconsistent again. | 2026-07-22 | — | — | `BUG_FOUND` |

---

## Roadmap Items (from `docs/roadmap-killer-todos.md`)

| # | Item | Priority | Status | PR |
|---|------|----------|--------|-----|
| ★2 | Specialized Sub-Agents with Per-Role Models | P0 | `TODO` | — |
| A1 | Hermes ChatML Prompt Format | P0 | `TODO` | — |
| A2 | Multi-Hop ReAct Loop | P0 | `TODO` | — |
| B1 | Nemotron Reward Model Scoring | P0 | `TODO` | — |
| C2 | Function Calling (OpenAI-compatible) | P0 | `TODO` | — |
| ★5 | Sandboxed Agent Execution | P1 | `TODO` | — |
| ★6 | Cost Analytics + FTS5 Memory + Constitution | P1 | `TODO` | — |
| A3 | Capability Registry + Dynamic Tool Discovery | P1 | `TODO` | — |
| A4 | Async Task Queue | P1 | `TODO` | — |
| A5 | Inter-Agent Message Bus | P1 | `TODO` | — |
| B2 | SteerLM Steering Tokens | P1 | `TODO` | — |
| B3 | Synthetic Training Data Pipeline | P1 | `TODO` | — |
| B4 | NeMo Guardrails | P1 | `TODO` | — |
| B5 | NIM Connection Pooling + Circuit Breaker | P1 | `TODO` | — |
| C3 | Streaming Delta Reconstruction | P1 | `TODO` | — |
| C4 | Chat History Persistence | P1 | `TODO` | — |
| C5 | Context Window Management | P1 | `TODO` | — |
| C6 | Prompt Caching | P1 | `TODO` | — |
| D1 | Helm Chart | P1 | `TODO` | — |
| D2 | Docker Compose Production Stack | P1 | `TODO` | — |
| D3 | OpenTelemetry Distributed Tracing | P1 | `TODO` | — |
| E1 | Cross-Harness Routing | P1 | `TODO` | — |
| E2 | Self-Healing Agent Doctor | P1 | `TODO` | — |
| F2 | MCP Server | P1 | `TODO` | — |
| G1 | Per-Model Cost Attribution | P1 | `TODO` | — |

---
