name: Agency Cycle

# Proactive AI-powered CI repair loop.
# Triggers every 2 hours AND immediately when the CI workflow fails.
# Uses NVIDIA NIM (qwen2.5-coder-32b) or Anthropic as fallback to analyse
# failing tests, apply code edits, verify green, and push fixes automatically.

on:
  schedule:
    - cron: '0 */2 * * *'   # every 2 hours
  workflow_dispatch:
    inputs:
      directive:
        description: 'Optional specific directive for the CEO'
        required: false
        default: ''
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  ceo-assess-and-dispatch:
    name: CEO Assessment & Agent Dispatch
    runs-on: ubuntu-latest
    # Security gates:
    # 1. workflow_run: only act on failures AND only on commits from THIS repo
    #    (never forks) to prevent untrusted code running with write permissions.
    # 2. All other triggers: always run.
    if: >
      github.event_name != 'workflow_run' || (
        github.event.workflow_run.conclusion == 'failure' &&
        github.event.workflow_run.head_repository.full_name == github.repository
      )

    permissions:
      contents: write
      issues: write
      pull-requests: write

    # Shared test environment — avoids repeating these in every step
    env:
      API_KEYS: "ci-test-key"
      ADMIN_EMAIL: "admin@llmrelay.local"
      ADMIN_PASSWORD: "WikiAdmin2026!"
      SECRET_KEY: "ci-test-secret-do-not-use"
      OLLAMA_BASE: "http://localhost:11434"
      LANGFUSE_SECRET_KEY: ""
      LANGFUSE_PUBLIC_KEY: ""
      LANGFUSE_HOST: ""
      ROUTER_HEALTH_CHECK_ENABLED: "false"
      MONGO_URL: "mongodb://localhost:27017"
      DB_NAME: "llm_wiki_dashboard_ci"

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          # For workflow_run on our own repo: check out the failing commit SHA.
          # This is safe because the fork gate above blocks untrusted branches.
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -q
          pip install openai anthropic -q

      - name: Configure git identity
        run: |
          git config user.email "agency-bot@local-llm-server"
          git config user.name "Agency Bot"

      - name: Run pytest baseline
        id: baseline
        run: |
          python -m pytest --tb=short -q --no-header --timeout=120 2>&1 | tee /tmp/test_baseline.txt || true
          EXIT=${PIPESTATUS[0]}
          echo "exit_code=${EXIT}" >> "$GITHUB_OUTPUT"
          FAILING=$(grep '^FAILED ' /tmp/test_baseline.txt | awk '{print $2}' | head -5 | tr '\n' ',' | sed 's/,$//')
          echo "failing=${FAILING}" >> "$GITHUB_OUTPUT"

      - name: Scan for security issues (bandit)
        id: security
        run: |
          pip install bandit safety -q
          bandit -r . \
            --exclude ".git,.venv,venv,node_modules,tests,frontend,remote-admin" \
            --format json --output /tmp/bandit.json -ll 2>/dev/null || true
          COUNT=$(python3 -c "import json; d=json.load(open('/tmp/bandit.json')); print(len(d.get('results',[])))" 2>/dev/null || echo 0)
          echo "findings=${COUNT}" >> "$GITHUB_OUTPUT"

      - name: CEO Assessment
        id: ceo
        run: |
          python3 - <<'PYEOF'
          import json, os

          failing = os.environ.get("FAILING_TESTS", "")
          sec_count = int(os.environ.get("SEC_COUNT", "0"))
          test_ok = os.environ.get("TEST_OK", "1") == "1"

          issues = []
          priority = []

          if failing:
            tests = [t for t in failing.split(",") if t]
            issues.append(f"❌ {len(tests)} failing test(s): {', '.join(tests[:3])}")
            priority.append("FIX_TESTS")

          if sec_count > 0:
            issues.append(f"🔒 {sec_count} security finding(s) from bandit")
            priority.append("FIX_SECURITY")

          if not issues:
            issues.append("✅ All systems nominal")

          assessment = {
            "issues": issues,
            "priority": priority,
            "test_pass": test_ok,
            "security_count": sec_count,
          }

          print(json.dumps(assessment, indent=2))
          with open("/tmp/assessment.json", "w") as f:
            json.dump(assessment, f)
          PYEOF
        env:
          FAILING_TESTS: ${{ steps.baseline.outputs.failing }}
          SEC_COUNT: ${{ steps.security.outputs.findings }}
          TEST_OK: ${{ steps.baseline.outputs.exit_code == '0' && '1' || '0' }}

      - name: Dispatch Dev Agent — Fix tests via NVIDIA NIM
        if: steps.baseline.outputs.exit_code != '0'
        id: agent_fix
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          AGENCY_MODEL: "qwen/qwen2.5-coder-32b-instruct"
        run: |
          echo "Failing tests: ${{ steps.baseline.outputs.failing }}"
          # || true: agent exits 1 when it cannot fix; we still want post-fix
          # pytest and issue-creation steps to run so the escalation path fires.
          python scripts/agency_fix.py /tmp/test_baseline.txt || true

      - name: Run tests after fix attempt
        if: steps.baseline.outputs.exit_code != '0'
        id: post_fix
        run: |
          python -m pytest --tb=no -q --no-header --timeout=120 2>&1 | tee /tmp/test_post.txt || true
          echo "exit_code=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"

      - name: Commit & push fixes
        if: steps.baseline.outputs.exit_code != '0' && steps.post_fix.outputs.exit_code == '0'
        # Pass user-controlled values through env vars, never inline in the shell
        # script, to prevent branch-name shell injection (CodeQL CWE-78).
        env:
          TRIGGER: ${{ github.event_name }}
          FAILING: ${{ steps.baseline.outputs.failing }}
          WR_HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
        run: |
          git add -A
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            # Resolve the target branch for detached-HEAD pushes.
            # workflow_run: head_branch comes from the triggering run.
            # All other triggers: GITHUB_REF_NAME is the current branch.
            if [ "${TRIGGER}" = "workflow_run" ]; then
              TARGET_BRANCH="${WR_HEAD_BRANCH}"
            else
              TARGET_BRANCH="${GITHUB_REF_NAME}"
            fi
            # Validate branch name using git's own rules.
            if ! git check-ref-format --branch "${TARGET_BRANCH}" > /dev/null 2>&1; then
              echo "ERROR: Unsafe branch name rejected: ${TARGET_BRANCH}"
              exit 1
            fi
            git commit -m "fix: agency auto-fix — ${FAILING} [trigger: ${TRIGGER}]"
            git push origin "HEAD:refs/heads/${TARGET_BRANCH}"
            echo "Pushed fixes to ${TARGET_BRANCH}"
          fi

      - name: Publish agency cycle summary
        run: |
          ASSESSMENT=$(python3 -c "import json; d=json.load(open('/tmp/assessment.json')); [print('-', i) for i in d['issues']]" 2>/dev/null || echo "Assessment unavailable")
          {
            echo "### Agency Cycle Results — $(date -u '+%Y-%m-%d %H:%M') UTC"
            echo ""
            echo "**Trigger:** \`${{ github.event_name }}\`"
            echo ""
            echo "**CEO Assessment:**"
            echo "$ASSESSMENT"
            echo ""
            echo "| Check | Result |"
            echo "|-------|--------|"
            TEST_ICON=$([[ "${{ steps.baseline.outputs.exit_code }}" == "0" ]] && echo "✅" || echo "❌")
            echo "| Tests | ${TEST_ICON} |"
            echo "| Security findings | ${{ steps.security.outputs.findings }} |"
            if [ "${{ steps.baseline.outputs.exit_code }}" != "0" ]; then
              POST_ICON=$([[ "${{ steps.post_fix.outputs.exit_code }}" == "0" ]] && echo "✅ Fixed" || echo "⚠️ Still failing")
              echo "| Post-fix tests | ${POST_ICON} |"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Create issue if agency cannot fix
        if: steps.baseline.outputs.exit_code != '0' && steps.post_fix.outputs.exit_code != '0'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const baseline = fs.readFileSync('/tmp/test_baseline.txt', 'utf8');
            const postFix = fs.existsSync('/tmp/test_post.txt')
              ? fs.readFileSync('/tmp/test_post.txt', 'utf8') : 'No post-fix output';

            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner, repo: context.repo.repo,
              labels: 'auto-detected,bug', state: 'open', per_page: 5,
            });
            if (issues.some(i => i.title.includes('Agency: Cannot Fix Tests'))) return;

            await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: `Agency: Cannot Fix Tests — ${new Date().toISOString().split('T')[0]}`,
              body: [
                '## Agency Escalation: Tests remain failing after automated fix attempt',
                '',
                '**Trigger:** `' + context.eventName + '`',
                '',
                '**Baseline failures:**',
                `\`\`\`\n${baseline.slice(0, 2000)}\n\`\`\``,
                '',
                '**Post-fix run:**',
                `\`\`\`\n${postFix.slice(0, 2000)}\n\`\`\``,
                '',
                '_Human review required. The Dev Agent could not resolve these automatically._',
              ].join('\n'),
              labels: ['bug', 'auto-detected', 'agency-escalation'],
            });
